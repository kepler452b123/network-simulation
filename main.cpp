#include "main.h"

void main()
{
    
    std::string operation;
    std::cout << "Enter a to add device, m to send a message between two devices";
    std::cin >> operation;
    if (operation == "a")
    {

    }
}
